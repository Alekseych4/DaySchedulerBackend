server:
  port: 5000
  servlet:
    context-path: /api/v1
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/day_scheduler?createDatabaseIfNotExist=true&useUnicode=true&serverTimezone=UTC
    username: alekseych
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format-sql: true
  liquibase:
    change-log: classpath:db/changelog/changelog-master.yml
logging:
  level:
    root: debug
